(display "HOME: ")
(display (getenv "HOME"))
(newline)

(display "USER: ")
(display (getenv "USER"))
(newline)

(display "SHELL: ")
(display (getenv "SHELL"))
(newline)

(display "TERM: ")
(display (getenv "TERM"))
(newline)
